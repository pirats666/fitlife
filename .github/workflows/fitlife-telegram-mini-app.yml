name: FitLife Telegram Mini App

on:
  push:
    branches: [main, feature/telegram-mini-app]
    paths:
      - 'apps/telegram-mini-app/**'
      - '.github/workflows/fitlife-telegram-mini-app.yml'
  pull_request:
    paths:
      - 'apps/telegram-mini-app/**'
      - '.github/workflows/fitlife-telegram-mini-app.yml'

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/telegram-mini-app/backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm run typecheck
      - run: npm run build

  bot:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/telegram-mini-app/bot
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npx tsc --noEmit
      - run: npm run build

  webapp:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/telegram-mini-app/webapp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node --check app.js
